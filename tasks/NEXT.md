# Próxima atividade

Nenhuma entrega sequencial está aprovada no momento.

A task mais recente concluída foi a **114 — Define o modelo de autorização
para o GitHub CLI (`gh`)** (ver `tasks/114-github-cli-authorization-model.md`),
nova subseção em `docs/architecture/security.md`. O `gh` continua só leitura
(fallback já em produção para status/CI de PR); expor ações mutáveis
(`pr create`/`merge`/`close`) fica registrado como item próprio em
`tasks/PENDENCIAS.md`.

Para escolher a próxima, veja os itens em aberto em `tasks/PENDENCIAS.md`.
