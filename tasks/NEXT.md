# Próxima atividade

Nenhuma entrega sequencial está aprovada no momento.

A task mais recente concluída foi a **117 — Smoke E2E de snapshot/restore
de banco de dados** (ver `tasks/117-e2e-database-snapshot-restore.md`):
`sample-rails-app` ganhou `config/database.yml` + binários `mysqldump`/
`mysql` fake no harness, e três specs novos cobrem gerar snapshot, restaurar
(com cancelamento) e a ausência da aba quando não há banco detectado.

Também foram removidos de `tasks/PENDENCIAS.md`, a pedido do usuário, itens
especulativos sem demanda real: estratégia específica para Windows,
manifesto declarativo de extensões, e contexto semântico via embeddings
locais/restauração de abas entre sessões.

Para escolher a próxima, veja os itens em aberto em `tasks/PENDENCIAS.md`.
