# Próxima atividade

Nenhuma entrega sequencial está aprovada no momento.

A task mais recente concluída foi a **111 — Varredura recursiva de
workspace: expor na API e na UI** (ver
`tasks/111-recursive-workspace-scan-api-ui.md`). Ficou faltando só uma tela
de edição de workspace na UI para alternar `recursiveScan` de um workspace
já cadastrado (hoje só cadastro novo, via checkbox, ou `PATCH` direto na
API) — registrado em `tasks/PENDENCIAS.md` ("Descoberta e projetos
complexos").

Para escolher a próxima, veja os itens em aberto em `tasks/PENDENCIAS.md`.
