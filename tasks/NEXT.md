# Próxima atividade

Nenhuma entrega sequencial está aprovada no momento.

A task mais recente concluída foi a **112 — Tela para alternar
`recursiveScan` de um workspace já cadastrado** (ver
`tasks/112-recursive-workspace-scan-edit-ui.md`), que fecha a frente aberta
pelas tasks 110 (varredura recursiva na biblioteca) e 111 (API + cadastro).

Para escolher a próxima, veja os itens em aberto em `tasks/PENDENCIAS.md`.
