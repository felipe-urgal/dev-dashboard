name: Worktree format debug

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 24
          cache: npm
      - run: npm ci --ignore-scripts
      - name: Show Prettier diff
        run: |
          npx prettier --write apps/web/src/components/ProjectWorktreesPanel.vue
          git diff -- apps/web/src/components/ProjectWorktreesPanel.vue
