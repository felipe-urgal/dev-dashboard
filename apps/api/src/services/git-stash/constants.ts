export const FIELD_SEPARATOR = '\u001f';
export const CONFIRMATION_TTL_MS = 60_000;
export const PATCH_LIMIT = 320_000;
export const STASH_REFERENCE_PATTERN = /^stash@\{(\d+)\}$/;
export const CONFLICT_PATTERN = /conflict|merge conflict|needs merge|could not restore untracked files/i;
